Global variables in nodejs. != from Window object in browser  | Il y a plus de global variables.

__dirname - path to current directory
__filename - filename
require - function to use modules (CommonJS)
module - info about current module (file)
process - info about env where the program is being executed    ---> This is particular usefull when we deploy our applications

If we execute a variable on a module when we require the module the function will execute.
When you import a module you invoque it.

Node have a lot of build in modules.

We install nodemon, we would ot have to reload each time our app.
We gonna install it in dev dependencies. npm i nodemon -D (--save-dev)
We don't need Nodemon in production we just need it for developpement.

It's important to put the folder with noe_modules in the .gitignore file